{
  "name": "my-defi-empire",
  "version": "1.0.0",
  "description": "DeFi Empire with flashloans and arbitrage",
  "main": "empire-ai.js",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy-skim": "npx hardhat run scripts/deploy-skim-router.js --network arbitrum",
    "deploy-empire": "npx hardhat run scripts/deploy-godmode-empire.js --network arbitrum",
    "run-ai": "node empire-ai.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.4",
    "dotenv": "^16.3.1",
    "@chainlink/contracts": "^0.6.1",
    "@openzeppelin/contracts": "^5.0.0"
  },
  "dependencies": {
    "ethers": "^6.14.0"
  }
}